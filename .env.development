# Dev-only overrides.
# The production Clerk proxy/custom-domain settings can cause Origin/Host mismatch errors on localhost.
# Setting these to empty disables them for `next dev` (values become falsy in runtime).
NEXT_PUBLIC_CLERK_DOMAIN=
NEXT_PUBLIC_CLERK_PROXY_URL=
CLERK_PROXY_URL=
CLERK_ACCOUNTS_URL=
